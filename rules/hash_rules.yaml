hash_rules:
  insecure_algorithms:
    md5:
      message: "MD5 is broken and should not be used."
      cwe: "CWE-327: Use of a Broken or Risky Cryptographic Algorithm."
      recommendation: "Use SHA-256 or stronger."
    sha1:
      message: "SHA-1 is weak due to collision vulnerabilities."
      cwe: "CWE-327: Use of a Broken or Risky Cryptographic Algorithm."
      recommendation: "Use SHA-256 or stronger."
  safe_algorithms:
    sha256:
      message: "SHA-256 is a secure cryptographic hash function."
      
    sha512:
      message: "SHA-512 is a secure cryptographic hash function."
      
    sha3_256:
      message: "SHA3-256 is a secure cryptographic hash function."
      
    sha3_512:
      message: "SHA3-512 is a secure cryptographic hash function."
      
  warnings:
    no_salt:
      message: "Secure hashing used, but ensure a salt is applied."
      cwe: "CWE-759: Use of a one-way hash without a salt can lead to rainbow table attacks."
    custom_hash:
      message: "Custom hashing detected, which may be insecure."
      cwe: "CWE-328: Insufficient Entropy in PRNG."
      recommendation: "Use a cryptographically secure hashing function instead."
